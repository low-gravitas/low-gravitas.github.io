{
  "name": "low-gravitas-hub",
  "version": "0.0.0",
  "private": true,
  "description": "lowgravitas.com — single Eleventy site consolidating landing, blog, zen theme demo, and symbol font glyph browser.",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "prebuild": "node scripts/fetch-artifacts.mjs && node scripts/stage-vendor.mjs && node scripts/verify-palette.mjs",
    "build": "eleventy",
    "dev": "eleventy --serve",
    "test": "node scripts/test-cache-bust.mjs",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^5.0.0",
    "@11ty/eleventy-plugin-rss": "^2.0.2",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.0",
    "markdown-it": "^14.1.0"
  }
}
